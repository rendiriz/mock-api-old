{
  "name": "express-esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "clean": "rimraf dist/*",
    "dev:start": "npm-run-all build start",
    "dev": "env-cmd nodemon --watch src -e ts --exec npm run dev:start",
    "start": "node dist",
    "build:eslint": "esbuild ./src --platform=node --bundle --minify --outfile=dist/index.js",
    "build": "npm-run-all clean build:eslint",
    "lint": "eslint ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "env-cmd": "^10.1.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.13",
    "config": "*",
    "esbuild": "^0.14.14",
    "eslint": "^8.8.0",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "tsconfig": "*",
    "typescript": "^4.5.5"
  }
}
